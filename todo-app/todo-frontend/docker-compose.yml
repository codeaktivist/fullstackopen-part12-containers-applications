version: '3.8'

services:
  app:
    image: hello-front-dev
    build:
      context: .
      dockerfile: ./dev.Dockerfile
    ports:
      - 3001:3001
    volumes:
      - ./:/usr/src/app
    container_name: hello-front-dev-container